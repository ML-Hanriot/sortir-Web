{% extends 'base.html.twig' %}

{% block title %}Ajouter une Ville{% endblock %}

{% block body %}
    <h1>Ajouter une ville</h1>

    {{ form_start(form) }}
    <div>
        {{ form_row(form.nom) }}
        {{ form_row(form.codePostal) }} <!-- Champ Code Postal -->
    </div>
    <button class="btn">Ajouter</button>
    {{ form_end(form) }}

    <a href="{{ path('villes_index') }}">Retour à la liste des villes</a>
{% endblock %}
